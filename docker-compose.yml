services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: cca_indexer
      POSTGRES_USER: cca
      POSTGRES_PASSWORD: cca
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
